def zip(s):
    if s =='': return ''
    if len(s)==1:
        s = s + '1'
        return s
    count=1
    cur = 1
    prev = 0
    if s[cur] == s[prev]:

    result
    return s[cur]

print(zip('as'))

exit(0)
